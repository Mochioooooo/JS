const numbers = [1, 2, 3];

const doubleNumArray = numbers.map((num) => {
    return num * 2;
});  //It means .map is for contain arrays. also num is a argument includes [1, 2, 3]

console.log(numbers);
console.log(doubleNumArray);